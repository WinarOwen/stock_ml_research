# Stock Prediction using Machine Learning research 

To install:
- Create a virtual environment in python ```python -m [virtual environement name here]```
- Clone the repository using git clone 
- Use pip install -r requirements.txt to install dependencies
- Run and mess around with the code
